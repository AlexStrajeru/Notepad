<UserControl x:Class="MyNotepad.Features.StatusBar.StatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#007ACC" Padding="10,4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding FileName}" Foreground="White" FontSize="12" Margin="0,0,16,0"/>
            <TextBlock Grid.Column="1" Text="{Binding FilePath}" Foreground="#CCCCCC" FontSize="12" Margin="0,0,16,0"/>
            <TextBlock Grid.Column="3" Text="{Binding LineCount}" Foreground="White" FontSize="12" Margin="0,0,16,0"/>
            <TextBlock Grid.Column="4" Text="{Binding DirtyStatus}" Foreground="White" FontSize="12"/>
        </Grid>
    </Border>
</UserControl>

